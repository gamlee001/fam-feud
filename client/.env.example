# URL of the deployed Socket.io game server (no trailing slash).
# Set this in your host's project Environment Variables for production builds.
# Leave unset for local dev (the client falls back to <host>:3001).
VITE_SERVER_URL=https://your-server.onrender.com
